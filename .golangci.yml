version: "2"

linters:
  enable:
    - revive
  settings:
    errcheck:
      exclude-functions:
        - (*os.File).Close
        - (io.ReadCloser).Close
    revive:
      rules:
        - name: var-naming
          disabled: true